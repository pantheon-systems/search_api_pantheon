{
    "name": "pantheon-systems/search_api_pantheon",
    "type": "drupal-module",
    "description": "Connection module for Pantheon's nextgen solr service",
    "license": "MIT",
    "repositories": [
        {
            "type": "composer",
            "url": "https://packages.drupal.org/8"
        }
    ],
    "config": {
        "process-timeout": 0,
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true,
        "platform": {
            "php": "8.0"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Drupal\\search_api_pantheon\\" : "./src",
            "Drupal\\search_api_pantheon\\tests\\" : "./tests"
        }
    },
    "extra": {
        "drush": {
            "services": {
                "drush.services.yml": "^10"
            }
        }
    },
    "require": {
        "php": ">=7.4",
        "ext-curl": "*",
        "ext-json": "*",
        "drupal/search_api_solr": "^4.2",
        "guzzlehttp/guzzle": "^6.5.2",
        "http-interop/http-factory-guzzle": "^1.0",
        "php-http/guzzle6-adapter": "^2.0"
    },
    "require-dev": {
        "drupal/coder": "@stable",
        "psy/psysh": "@stable",
        "kint-php/kint": "@stable",
        "phpunit/phpunit": "@stable",
        "squizlabs/php_codesniffer": "@stable"
    },
    "scripts": {
        "code:test" : [
            "phpunit -c ./phpunit.xml"
        ],
        "code:fix": [
            "vendor/bin/phpcs --config-set installed_paths vendor/drupal/coder/coder_sniffer",
            "phpcbf --standard=Drupal,DrupalPractice src/* tests/Unit/*"
        ],
        "code:lint": [
            "vendor/bin/phpcs --config-set installed_paths vendor/drupal/coder/coder_sniffer",
            "phpcs --standard=Drupal,DrupalPractice src tests/Unit"
        ],
        "pre-commit": [
            "composer validate --strict",
            "@code:fix",
            "@code:lint",
            "@code:test"
        ]
    }
}
